# Local name

Title: **Local name**. The editable value is optional. Explain: “This name stays only on this device.” Save/removal affects only local display and has no sync, profile, server, or peer effect.

Without an alias, show a locally derived abbreviated established-peer representation approved by the future secure-session design. Never use an expired rotating contact ID as a lasting conversation identity.
