# Reset secure relationship

Title: **Reset secure relationship?**

Copy: “The current session and routing state will be discarded. You will need to exchange current IDs again.” Actions: **Cancel** and destructive **Reset**.

This is not “refresh encryption.” It may retain the local conversation shell and alias, but old cryptographic/session state cannot resume.
