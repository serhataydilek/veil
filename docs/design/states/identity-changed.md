# Identity changed

Pause normal conversation trust and place a clear in-context notice: `Identity changed` / `This person's cryptographic identity is different from the one previously verified.` Actions are **Verify**, **Reset relationship**, and **Block**.

Do not offer normal send flow until the existing state-machine requirements are met. The copy does not state that an attack occurred; legitimate identity replacement is possible, but trust must not continue silently.
